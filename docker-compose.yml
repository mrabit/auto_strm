services:
  app:
    build: .
    container_name: auto_strm
    ports:
      - "${WEB_PORT:-28491}:3000"
    environment:
      - WEB_PORT=3000
      - TZ=Asia/Shanghai
    volumes:
      - ./config:/app/config
      - ./data:/data
    restart: unless-stopped
